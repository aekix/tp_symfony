{% extends 'base.html.twig' %}

{% block title %}Mes addrticles{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <ul>
    {% for user in users %}
        <li> {{ user.lastname  }}<a href="{{ path('profile', {'id':user.id}) }}">edit</a> <a href="{{ path('user_remove', {'id': user.id}) }}">remove</a></li>
    {% endfor %}
    </ul>
{% endblock %}
